import { PositiveValidatorDirective } from './positive-validator.directive';

describe('PositiveValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
